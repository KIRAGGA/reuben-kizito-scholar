import { PageHero } from "@/components/page-hero";
import { getPublicProfile } from "@/lib/content-server";

export default async function CommunityPage() { const profile = await getPublicProfile(); return <><PageHero eyebrow="Community work" title="Enterprise is also a way of serving people." intro="Community work brings the same attention to sustainability, capability and dignity that guides the research." /><section className="section"><div className="container split"><div><p className="statement">The strongest community initiatives create value that can keep moving after the initial support has ended.</p></div><ul className="list-clean">{profile.community.map((item) => <li key={item}>{item}</li>)}</ul></div></section><section className="section section-dark"><div className="container narrow"><p className="eyebrow">A practical commitment</p><p className="lead">Community health, social enterprise and local capability are not side topics. They are part of the question of what sustainable development looks like in everyday life.</p></div></section></>; }
