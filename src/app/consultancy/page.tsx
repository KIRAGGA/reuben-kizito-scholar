import { PageHero } from "@/components/page-hero";
import { getPublicProfile } from "@/lib/content-server";

export default async function ConsultancyPage() { const profile = await getPublicProfile(); return <><PageHero eyebrow="Consultancy" title="Strategy with enough room for the real world." intro="Practical support for small and medium enterprises, social ventures and institutions building better ways to work." /><section className="section"><div className="container cards-grid">{profile.consultancy.map((item, index) => <article className="info-card" key={item}><p className="eyebrow">0{index + 1}</p><h3>{item}</h3><p>Clarify the current situation, identify the highest-leverage decision and build a workable next step.</p></article>)}</div></section><section className="section section-tint"><div className="container split"><div><p className="eyebrow">How the work begins</p><p className="statement">Good consulting starts by listening long enough to understand the system around the immediate problem.</p></div><div className="article-copy"><p>Past assignments have supported business development, human resource management, entrepreneurship, strategic management, marketing, consumer behaviour and product development.</p><p>For a new engagement, the first conversation is used to understand the context, the people involved and the outcome that would make the work worthwhile.</p><a className="button" href="/appointments">Start a conversation</a></div></div></section></>; }
