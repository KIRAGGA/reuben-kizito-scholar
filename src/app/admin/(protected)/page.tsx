import Link from "next/link";
import { getAllPublications, getPublicProfile } from "@/lib/content-server";

export default async function AdminDashboardPage() { const profile = await getPublicProfile(); const pubs = await getAllPublications(); return <section><p className="eyebrow">Overview</p><h1>Good work starts with a clear home base.</h1><p className="lead-small muted">Edit the public scholar profile, keep the publication catalogue current and respond to appointment requests.</p><div className="admin-grid"><div className="admin-card"><strong>{pubs.length}</strong><h3>Catalogue records</h3><p>Published, book and under-review items.</p></div><div className="admin-card"><strong>{profile.researchInterests.length}</strong><h3>Research interests</h3><p>Displayed throughout the public site.</p></div><div className="admin-card"><strong>UTC+3</strong><h3>Appointment timezone</h3><p>East Africa Time, configured for Kampala.</p></div></div><div className="cards-grid"><div className="info-card"><h3>Edit profile</h3><p>Update the introduction, tagline, quote and contact presentation.</p><Link className="text-link" href="/admin/settings">Open content editor →</Link></div><div className="info-card"><h3>Manage publications</h3><p>Add articles, books and manuscripts while keeping review status clear.</p><Link className="text-link" href="/admin/publications">Open catalogue →</Link></div><div className="info-card"><h3>Review appointments</h3><p>Approve, decline or cancel requests after reviewing their context.</p><Link className="text-link" href="/admin/appointments">Open requests →</Link></div></div></section>; }
