import { AdminLoginForm } from "@/components/admin-login-form";

export const metadata = { title: "Admin login" };
export default function AdminLoginPage() { return <main className="admin-shell"><div className="container narrow"><p className="eyebrow">Private administration</p><h1>Sign in to the content studio.</h1><p className="lead-small muted">Manage the public profile, publications and appointment requests from one place.</p><AdminLoginForm /></div></main>; }
import { AdminLoginForm } from "@/components/admin-login-form";

export const metadata = { title: "Admin login" };
export default function AdminLoginPage() { return <main className="admin-shell"><div class="container narrow"><p className="eyebrow">Private administration</p><h1>Sign in to the content studio.</h1><p className="lead-small muted">Manage the public profile, publications and appointment requests from one place.</p><AdminLoginForm /></div></main>; }
